# Runtime dependencies
pandas>=1.5
numpy>=1.24
matplotlib>=3.6
scipy>=1.10
rasterio>=1.3
pyodbc>=4.0

# Build / packaging
pyinstaller>=5.10

# Note: `tkinter` (GUI) is provided by the Python installation on Windows
# and does not need to be installed with pip.
# Run "python -m pip install -r .\requirements.txt" to install these dependencies.